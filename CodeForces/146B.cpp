#include<bits/stdc++.h>
using namespace std;
int lucky(int x){
    string s = to_string(b);
    
}
int main(){
    int a, b, c;
    cin >> a >> b;
    string s = to_string(b);
    int d = a % (10*s.size());
    if(d <= b){
        c = a - d + b; 
    }
    else{
        c = a + (10*s.size()) + 
    }
    return 0;
}